import * as zIndexes from './zIndexes';
import * as SETTING_KEY from './setting-key';
import { CELL_TYPE } from './cell-constants';

const PLUGIN_NAME = 'gallery';

export {
  PLUGIN_NAME,
  zIndexes,
  SETTING_KEY,
  CELL_TYPE
};